# v1.0.1 — dyson-tree (2026-09-11)

**Read this cold.** v1.0.0 shipped and was audited by a 10-judge review panel; every item
below was then verified by the coordinator against the released tree at `4d4d8a7`. You
are the executor; the coordinator owns every remote operation. Branch `release/1.0.1-rc`
from the current `master`. Push ONLY that branch.

## Scope allowlist — read twice

You MAY correct statements, add committed scripts that _reproduce an already-reported
number from committed data_, re-run the three experiment runners **only** to refresh their
provenance headers (item 5 — the data must come back byte-identical, and the
regenerate-exactly tests prove it), fix docs/metadata/links. You MAY NOT change `sim/`,
presets, constants, pre-registrations, or add inputs to the explorer. **A fix changes what
is SAID, never what was MEASURED.**

## Punch list

### A. Claim-level

1. `docs/FINDINGS.md:34-40` (the Q2b three-candidate table) says "Lives in:
   `experiments/q2b_adapted/limits.csv`", which has only `class,omega,outer_au,binding`.
   The temperature (1.1945 AU) and light (70.04 AU) candidates exist in no committed file.
   Write `tools/derive_q2b_candidates.py` that computes the table from committed inputs
   (`sweep.csv` + `classify_limit`), commit its output as
   `experiments/q2b_adapted/candidates.csv` with a provenance header, and cite that file.
   Do the same for every other FINDINGS number with no artifact: the 66.4% / 43.3% /
   [11.04, 14.68] AU reachability scan (FINDINGS:247-250, CHANGELOG:67-68), the Gaussian
   0.0037 / net carbon +0.0197 at 1.1945 AU (FINDINGS:57-58), and the vascular
   `classify_limit` table (FINDINGS:99-105). Then FINDINGS:19's rule ("every number names
   the file it lives in and the command that produces it") is true; if any number cannot be
   given a committed producer, soften the rule sentence instead of leaving it false.
2. Algal-only scope. `web/index.html:136-137` "carbon was predicted to bind, and
   temperature binds instead" → add "for the algal class; vascular would have carbon bind
   at Ω = 25, 30 (see FINDINGS)". Same sentence unscoped at `CHANGELOG.md:53` and
   `docs/thermal_premise_retired_2026-09-03.md:99` — scope both.
3. Stale numbers far from their banner. `docs/ROADMAP.md:41-42` (13.69 / 67.26 AU →
   12.7058 / 62.4490), `experiments/q2b_adapted/RESULTS.md:109-115` (table 1.2120…2.5301 vs
   committed 1.2119…2.5289 — the banner names only the light limit), `RESULTS.md:123`
   (67.2623), `CHANGELOG.md:41-42` (`(N+1)^0.25` without the τ = 1 qualifier FINDINGS
   attaches). Rule for this release: **an inline `⚠️ superseded → <value> (S5)` marker at
   the stale number itself**, in addition to any top banner. Extend the q2b banner to say the
   whole §4 table is pre-S5.
4. `docs/FINDINGS.md:181` "Q3 was registered to design it" contradicts `:9` ("premise
   falsified before it could be registered"). Fix :181.
5. Provenance headers: every experiment CSV names `git_sha=fc6ba42`, whose code cannot
   produce the data. Re-run `experiments/q1_crossover/run.py`, `q2_thermal/run.py`,
   `q2b_adapted/run.py` at the RC commit under Python **3.13.2** and commit the CSVs. The
   only diff allowed is header lines (`git_sha`, `written`); if any data row differs, STOP
   and hand back. Update FINDINGS:287-303 and RELEASE §7.4 item 3 to say this was done.

### B. README + reproduce

6. `README.md` has no install/test/build block; it opens with "The brief, as filed" and
   loops A–D. New top: one paragraph, the explorer link, FINDINGS link, licence line, then
   a **Run it** block (clone → `python3 -m venv` → `pip install -e '.[dev]'` → `python -m
pytest -q` → `tools/build_site.sh`) that states: Python **3.13.2** exactly (the
   regenerate-exactly guards diff a `# python=` header; other 3.13.x patches fail three
   tests on the header alone — say so); Node ≥ 18 for the 9 parity tests; Playwright +
   Chromium for `tools/smoke_page.py`; R 4.3.3 + ggplot2 4.0.2 for figures. Move the
   design-history sections below the fold under "Design record". Fix Layout: preregs live
   in `experiments/*/prereg.yaml`, not `docs/`; delete the `_scratch/` line.
7. `docs/FINDINGS.md:350` `python >= 3.13` → `3.13.2` with the same one-line reason.

### C. Metadata / hygiene

8. README licence partition omits `experiments/**/prereg.yaml` (LICENSE-docs includes
   it) — align. Add a one-line exception note at the top of `LICENSE` pointing at
   `LICENSE-docs` for documentation. `CITATION.cff`: add `license-url` for both licences.
9. `experiments/q2b_adapted/prereg.yaml:75` cites `.superpowers task-3-brief.md`, not in
   the repo. Add a comment line after it: "(internal working note, not shipped; the
   measurement is reproduced by `<command>`)" — find the command or say none exists.
10. `docs/FINDINGS.md:163` "missed by roughly a factor of two" — actual misses are 1.2–1.8×
    (0.3939 vs [0.60, 0.70]; 0.2424 vs [0.30, 0.40]); write "by up to ~1.7×". FINDINGS:209
    "factor of 701" from endpoints 0.00074 / 0.52061 (= 703.5) — quote the unrounded ratio's
    source or change to "~700".
11. `docs/RELEASE-1.0.md:585,751` list "README references `_pm/`…" as a surviving defect;
    the shipped README has none (fixed post-log). Annotate both lines "fixed by the
    coordinator before the flip". Add a top banner: "Release evidence log, not a findings
    document."
12. `docs/superpowers/plans/README.md` (new, 5 lines): what the plan files are (agent
    execution checklists kept as a design record), that the tool names in them are the
    author's private tooling.
13. `CHANGELOG.md`: `## [1.0.1] — 2026-09-11` listing every item above.

## Acceptance (record in `docs/RELEASE-1.0.1.md`)

- `python -m pytest -q` all green (131 + any new tests) under 3.13.2; regenerate-exactly
  guards green after item 5
- `tools/build_site.sh` + `python3 tools/smoke_page.py` 147/147
- every number changed cites its committed artifact; every derived CSV has a header naming
  the script + commit
- ghostcite on `docs/dois.txt` clean; gitleaks history + `site/` clean; no `/home/`,
  `-home-`, or Windows account strings in the tree
- one fresh-critic pass on the rendered page + FINDINGS; non-waivable = a displayed
  number disagreeing with `fixtures.json`, an unscoped falsification claim, a missing link
- handoff: RC SHA, per-item disposition table, evidence

Push `release/1.0.1-rc` and stop.
