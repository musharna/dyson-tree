# Executor brief: dyson M3, the picture (SVG cross-section).

Repo `/home/mjarnold/dyson-tree`, master `4f3a1c1`. Authority: spec §5 "The picture" paragraph and §1, §8 M3 bullet,
§9 risk 3 (`docs/superpowers/specs/2026-09-12-northstar-design.md`). Read fresh; spec wins.

- SVG cross-section on the left of the page, driven ONLY by `DysonModel` outputs (no physics in the drawing code):
  outer circle ∝ R + t, inner ∝ R, wall band at true t/R, clamped to a visible minimum with a visible clamp label;
  interior hue from the transmitted PAR spectrum (τ(λ,t) over the 31 PAR rows), brightness from f_photon; organism
  a green disc, saturation = net_carbon_contained / a_max, grey at 0. Overlays keyed to each inequality: BURST fractures
  the wall (p·R/2t vs σ_eff(T_shell) printed at the break), FREEZE frosts (T_int vs T_freeze), BOIL dries (p vs p_sat),
  STARVE fades the disc (net vs 0), OPAQUE darkens (f_photon vs f_floor, labelled declared). The number that killed it is
  printed beside the wound. No text-transform. Keep a11y (title/desc on the SVG, live region already exists).
- Acceptance, measured from the DOM by `tools/smoke_page.py` (and a node test): unclamped state R 1 km, manual t 50 m
  (t/R 0.050): drawn wall-band pixel thickness / inner radius equals the readout's t/R to 2 dp; at defaults
  (t/R 1.45e-3) the clamp is active and the clamp label is present. Also assert each overlay appears exactly when its
  line is VIOLATED (use M2's gestures: r past r_close → frost only; manual p > p* → fracture only; p < p_sat → dry only;
  manual t 100 m → dark only), and none at defaults. Geometry read back from the DOM, never from the code's intent.
- Render a PNG screenshot per state (defaults, clamped/unclamped, each overlay) into `docs/m3_shots/` for the
  independent visual critic; M3 passes only through that critic, not your own read.
- Known: edge searches block the main thread ~1 s after a drag (M2); do not make it worse; report input→picture latency.
- Branch `m3-picture`; commit as you go; no push/merge; no attribution; prereg md5 d8707a66; tests seen failing first;
  `pytest -q`, `-m slow`, `tools/smoke_page.py` green. Report: SHAs, counts, measured ratios, shots list, latency, gaps.
