# Executor brief: dyson spec pass 6 (round-6 fixes), then prereg.yaml, then the hand rows on top.

Repo `/home/<user>/dyson-tree`, master `ae56554`+brief. Authority: spec
`docs/superpowers/specs/2026-09-12-northstar-design.md` (25c5f37, 1177 lines) as amended by THIS brief;
the codex round-6 review `~/.cache/llm-panel/runs/dyson-tree-6e0926de3dc8/20260922-205412-2638298/codex.md`
(read it in full). User approved: fix, then commit the prereg, NO further audit round.

## Step 1 — spec revision (one commit on master: "docs(northstar): revise after round-6 audit")
1. **B1 arm (ii) energy balance.** Replace `T_int⁴ = (1 − R_slab)(1 + T)·T_eq⁴` (and every equivalent
   phrasing, incl. "scales T_eq by (1−R_slab_sw)^¼" applied to T_int) with the two-body balance:
   `T_shell⁴ = (1 − R_slab_sw)·T_eq⁴`, `T_int⁴ = (1 − R_slab_sw + T_sw)·T_eq⁴`; state the derivation in
   two lines (interior: T_int⁴ = T·T_eq⁴ + T_shell⁴; shell: 2T_shell⁴ = A·T_eq⁴ + T_int⁴, A = 1−R−T) and
   that it reduces to the registered (1+τ) at R = 0. Replace the eight arm rows with these VERIFIED
   values (`/home/<user>/.claude/jobs/<job>/tmp/v7-dyson/v7_slab.out`, script `v7_slab.py` beside it):
   P1 (ii) vapour 1.1540 / 1.1766 / 1.1980 AU, water 1.1668 / 1.1914 / 1.2146 (variant (i) rows and all
   doors unchanged); P2 (ii) vapour FREEZE 78.4 km (t 34.2 m) before OPAQUE 102.3 km, water OPAQUE
   109.8 km before FREEZE 111.6 km (binding OPAQUE, margin 1.8 km — say it is the closest pair in the
   arm); vac(ii) defaults p* 1391.3 Pa, T_int 285.03 K, T_shell 259.89 K if the spec prints a defaults
   row under (ii). Fix every prose sentence that states the old consequences (e.g. "FREEZE below the
   60 km floor", "flips binding" for water). grep the spec for 1.1430, 1.1637, 1.1831, 1.1573, 1.1805,
   1.2025, 57.1, 108.3, 86.0, 115.6 and resolve EVERY hit (report the list).
2. **B2 tie clause.** Candidates = lines that HOLD at the last all-HOLD node and VIOLATE at the first
   any-VIOLATED node (the only lines with a sign change in the bracket); bisect each; the edge is the
   smallest root; roots within tolerance → load order, both printed; later roots of other lines are
   printed as counterfactual, never as the edge.
3. **M3 registry.** Add a twelfth row `solar_tail_transmission: band_weighted_tau_sw` (the 0.95% of TSI
   outside 280–4000 nm transmits like the band); rename "the law" key to `optical_law`; state the
   concrete `sim/vessel.py` signature carrying `optical_law`, `n_interior`, `thermal_reflectance`.
4. **M4 prereg tests (§6/§8 M1a0).** `test_prereg_equals_spec` compares the WHOLE prereg structure
   (bands, bindings, grids, edge rule, tolerances, load order, registry, arm rows) to a canonical
   expected structure parsed from the spec's own tables, not two band strings; drop the "ancestor of
   HEAD" half as tautological (keep: the prereg's introducing commit is a strict ancestor of the commit
   that introduces `tools/q4_hand_rows.py`); M1a0 acceptance runs the census (`pytest -m slow` named in
   the acceptance command). Say the hand-row commit is cherry-picked onto the prereg commit.
5. **MINOR6 stale wording:** THIRD-PARTY.md:35 already pinned; regeneration is parsed-value equality at
   rtol 1e-12, not byte-identical (:499-503, :678); `extract_ice_k.py` PAR filter is :37 and `--table`
   exists; `test_reproduce_hand_rows` vs the separate slow census test — describe what exists.
6. Header: a seventh-revision line (terse) + "Round 6 responses" section at the end, one line per
   finding. Use asserted replacements (script that asserts each old string occurs exactly once).

## Step 2 — prereg (its own commit, ALONE: only `experiments/q4_vessel/prereg.yaml`)
Written by hand from the revised spec (never generated by code that computes rows), referencing the
spec commit from step 1: both bands, both bindings, the r and R grids, edge rule incl. the new tie
clause, tolerances, load order, the twelve-row registry, the eight arm rows, the spectral table
digest/row count.

## Step 3 — hand rows on top
Cherry-pick `abfda84` (branch `m1a0-table`) onto the prereg commit; update `tools/q4_hand_rows.py`
arm (ii) to the conserving law, regenerate `.out`, update tests; add `test_prereg_equals_spec` (whole
structure), the strict-ancestor test, and make the smell test cover the prereg. Every new/changed test
seen to FAIL for its reason first (plant the old 1.1430 / old tie behaviour / swap commit order).
`pytest -q` AND `pytest -m slow` green. Do this on a branch `m1a0-prereg`; steps 1–2 commits also on
that branch (do not commit to master). No push, no merge, no attribution lines, no history rewrite of
master, do not delete `m1a0-table`.

Final report: commit SHAs in order, the grep-hit list for step 1, test counts (default + slow),
failing-first evidence, and anything that did not match (a mismatch is a finding).
