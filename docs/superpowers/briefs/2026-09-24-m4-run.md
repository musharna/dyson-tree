# Executor brief: dyson M4, the registered run.

Repo `/home/<user>/dyson-tree`, master `610ed8f`. Authority: `experiments/q4_vessel/prereg.yaml` (md5 d8707a66, committed
alone at ab1030e) and spec §6 + §8 M4 (`docs/superpowers/specs/2026-09-12-northstar-design.md`). Read both fresh; the prereg wins.

- THE PREREG IS FROZEN. Never edit it, never re-derive a band, grid, tolerance or edge rule from results. Score P1 and P2
  against its bands exactly; a miss is FAILED and is reported as FAILED. If the runner cannot implement a prereg clause as
  written, STOP and report — do not reinterpret it.
- `experiments/q4_vessel/run.py`, patterned on `experiments/q1_crossover/run.py`: gates A and C first (exit 2 on gate
  failure, nothing else written); BURST/BOIL asserted on the auto path (violation = exit 2); grids, edge rule (bracket,
  candidates, bisection, smallest root, ties, counterfactual later roots, empty window) and tolerances from the prereg;
  imports `sim/vessel.py` only (no deck). Outputs with provenance headers (git sha, prereg md5, spectral table sha256,
  vessel/physiology md5s, python/numpy versions): `sweep.csv` (the registered grid), `edges.csv` (P1 per σ, P2, controls,
  variants, every Fresnel-arm row), and `RESULTS.md` (each prediction: band, measured edge, binding, HELD/FAILED; control
  and arm rows beside the prereg's expected values with the difference). Cap worker count; estimate runtime first.
- Figures: R + ggplot2 via `experiments/q4_vessel/plot.R`, sourcing `docs/analysis/theme.R` (theme_dyson), one graph per
  figure; every result table gets a figure. FINDINGS gets a §5 section for Q4.
- Page: the `r_close` band drawn hatched as Q1's is (`web/app.js:118-119`), the measured `r_close` beside it, labelled HELD
  or FAILED from the committed results (not recomputed on the page). README opening paragraph counts four questions.
- Tests: a test that the results file was produced from the committed prereg md5 and code shas; a test that the page's
  HELD/FAILED label matches `RESULTS.md`; each seen failing first. `pytest -q`, `-m slow`, `tools/smoke_page.py` green.
- Branch `m4-run`; commit as you go; no push/merge; no attribution. Final report: SHAs, runtime, P1/P2 measured vs band
  with HELD/FAILED, control/variant/arm deltas vs prereg, any row off by more than its tolerance, counts, figure list.
