# Executor brief: M1a, the vessel in Python.

Repo `/home/<user>/dyson-tree`, master `509dfac`. Authority: the spec's §8 M1a bullet
(`docs/superpowers/specs/2026-09-12-northstar-design.md:721-750`), with §3-§6 for every equation and row.
Implement that bullet in full; the spec wins over this brief.

- Branch `m1a-vessel`; small commits; no push/merge; no attribution lines.
- `sim/vessel.py` reads `sim/spectral_table.csv`; it NEVER opens `experiments/q4_vessel/prereg.yaml` or
  `tools/q4_hand_rows.out`, and writes no band string (the import-guard test enforces it).
- `prereg.yaml` must stay byte-identical (md5 before = after).
- Reproduce every §6 row, control row, `central` row and the eight Fresnel arm rows (arm (ii) is the
  conserving law `T_int⁴ = (1 − R_slab_sw + T_sw)·T_eq⁴`, `T_shell⁴ = (1 − R_slab_sw)·T_eq⁴`) to the
  printed digits — compare against `tools/q4_hand_rows.out`'s values in TESTS only (tests may read it;
  `sim/` may not). A digit mismatch is a finding, never a tolerance widening.
- TDD: every test seen to fail for its reason first (report the evidence for the tie-rule, BURST
  invariant, import guard and 0.99·t_min mutation). `pytest -q` and `pytest -m slow` green.
- Stay in this repo. Final report: SHAs, test counts, every row compared (pass/fail), mismatches.
