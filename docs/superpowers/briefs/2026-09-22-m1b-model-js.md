# Executor brief: M1b, the vessel numbers in the browser.

Repo `/home/mjarnold/dyson-tree`, master `231e3e4`. Authority: spec §8 M1b bullet
(`docs/superpowers/specs/2026-09-12-northstar-design.md:750-754`) plus §3-§6; `sim/vessel.py` is the
reference implementation (all rows verified in M1a). Spec wins over this brief.

1. Port `sim/vessel.py` to `web/model.js` (existing `DysonModel` namespace; read the file's current
   conventions first): every public function incl. `classifyFailure`, the three arm inputs as options,
   σ_eff(T_shell), the tie rule, the spectral table (load it the way the page can — say how, and add any
   new file to the site build/allowlist).
2. `tools/make_fixtures.py` gains vessel cases (every §6 row, both closed-form controls, arm rows, the
   prereg's example inputs, BURST-inside-melt-door, a low-pressure case near p* 0.12 Pa at 2.82 AU);
   `tests/test_parity_js.py` covers them, `vessel` added to its md5 tuple (`:86-90`).
3. Acceptance: `DysonModel.classifyFailure(...)` at the prereg's example inputs returns the set the
   prereg prints — as a test driving the built page JS under node, and a real-browser check of the built
   site (open it, call it in the console context, record the output).
4. Spec sentence (same branch, separate commit): the fixed-point convergence is `min(1e-6 Pa, 1e-9·p)`
   because a 1e-6 Pa residual exceeds BOIL's 1e-6·p allowance below ~1 Pa (M1a finding, r 2.82 AU).
5. `web/fixtures.json` currently records `git_describe: "m1a-vessel"`; regenerate fixtures on this
   branch so it records something sane and say what.
- Branch `m1b-model-js`; no push/merge; no attribution. `prereg.yaml` md5 d8707a66 unchanged.
  Parity tests seen failing first (perturb one JS constant). `pytest -q` + `-m slow` green; node tests
  if any. Final report: SHAs, counts, parity cases, browser console output, anything that did not match.
