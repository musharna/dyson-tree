# Executor brief: dyson M2b, the deck (table, three tests, badge).

Repo `/home/<user>/dyson-tree`, master `b5e38d3`. Authority: spec §12 (`docs/superpowers/specs/2026-09-12-northstar-design.md`,
"The speculative tier", the rule-as-tests paragraph, "Gene cards", and the "Milestone" paragraph: M2b), §3, §4, §6 boxes.
Read fresh; spec wins over this brief.

- Ship ONLY the two live rows: antifreeze proteins (`T_freeze_K` down by `dT_sc`, TOTAL cap 1.3 K across stacked cards,
  clip printed) and reduced dark respiration (`r_d` down to 0.024, never zero; DECLARED). Every other row waits for its chapter.
  No material-table rows. No `f_floor` card.
- `web/deck.json` = the table, rows `{organism, trait, input, delta_or_range, cost, anchor, source}`; `sim/deck.py` mirrors it
  with a parity test. `sim/vessel.py` and `classify_failure` must NOT import the deck (test, both directions, as the existing
  import guard does). A DECLARED card applied to a registered run raises in `sim/deck.py`; the page allows it.
- The three tests, each seen FAILING first on a deliberately bad row/fixture, each with a positive control in the same test:
  1. every row names an input that exists; SEMANTIC comparator test: the set of comparators {T_freeze, f_floor, σ, p_sat, 0}
     a card can move is exactly {T_freeze} for antifreeze and empty for r_d (a name test is not acceptable, spec round 5).
  2. every MEASURED row's source resolves to a real file:line in this repo (no MEASURED row ships now; the test must still
     reject a bad MEASURED fixture and accept a good one).
  3. third rule over each card's whole reach (endpoints, max stack): after play, FREEZE is still FIRST violated inside its box
     (R 10 km, r in [1.19, 1.27] AU, every σ) and OPAQUE inside its box (r 1.10 AU, σ 0.7, R in [60, 300] km), and every
     failure stays reachable in the slider ranges; BURST/BOIL manual doors intact; STARVE margin still displayed at r_d 0.024.
     Must reproduce the spec's worked numbers: dT_sc 5 K FAILS (edges 1.2631/1.2894/1.3141); 0.8 K alone passes
     (1.2140/1.2395/1.2636), 2×0.8 fails (sigma 3.1 edge 1.2730); 1.3 K passes (1.2197/1.2453/1.2695); r_d 0.024 moves the
     P2-window STARVE margin +3.11 → +3.20. Mark slow if the edge searches need it.
- The page: the deck beside the inputs, each card with anchor badge (MEASURED/DEMONSTRATED/DECLARED), playing a card moves the
  input through `DysonModel`; the five lines respond as to any input. Keep a11y (labels, live region).
- Antifreeze is DEMONSTRATED: its source must be a real, verified citation (CrossRef JSON: first-author surname + year + DOI
  match, abstract supports "a few K" of freezing-point depression). If you cannot verify one, STOP and report; never invent.
- Branch `m2b-deck`; no push/merge; no attribution; prereg md5 d8707a66 unchanged; `pytest -q`, `-m slow`,
  `tools/smoke_page.py` green. Final report: SHAs, test counts, the red→green evidence per test, the worked numbers as
  reproduced, the citation with its CrossRef check, any mismatch with the spec.
